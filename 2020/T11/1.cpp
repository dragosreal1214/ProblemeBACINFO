#include<iostream>
#include<cmath>
using namespace std;
void patrate(int a, int b);
int main()
{
    patrate(10, 50);
}
void patrate(int a, int b)
{
    bool exista = false;
    int s = 0;
    
    for(int i = a; i <= b; i++)
    {
        if(sqrt(i) == int(sqrt(i)))
            
    }
}